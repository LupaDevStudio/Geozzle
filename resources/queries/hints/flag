# Replace Q_country

SELECT DISTINCT ?unicodeCharacter
WHERE {
  wd:Q_country wdt:P163 ?flag.
  ?flag wdt:P487 ?unicodeCharacter.

  SERVICE wikibase:label { bd:serviceParam wikibase:language "[AUTO_LANGUAGE],en". }
}
