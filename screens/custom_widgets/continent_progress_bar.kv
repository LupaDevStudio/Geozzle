#:kivy 2.2.1
#:set OUTLINE_RECTANGLE 6

<ContinentProgressBar>:

    # Background
    canvas.before:
        # Shadow
        Color:
            rgba: 0, 0, 0, 1        
        BoxShadow:
            pos: (0, 0)
            size: self.size
            offset: 5*self.font_ratio, -5*self.font_ratio
            spread_radius: -10*self.font_ratio, -10*self.font_ratio
            blur_radius: 20*self.font_ratio
            border_radius: [12*root.font_ratio,12*root.font_ratio,12*root.font_ratio,12*root.font_ratio]
        # Rounded rectangle
        Color:
            rgba: root.background_color
        RoundedRectangle:
            pos:(0, 0)
            size:self.size
            radius: [12*self.font_ratio]
        # Border
        Color:
            rgba: root.color
        Line:
            rounded_rectangle: [0,0,self.size[0],self.size[1],12*self.font_ratio]
            width: BUTTON_OUTLINE_WIDTH*self.font_ratio
    
    # Continent name 1
    Label:
        text: root.short_continents_list[0]
        font_size: root.font_ratio * root.font_size
        size_hint: (1/6, 0.8)
        pos_hint: {"x": 0/6, "center_y": 0.5}
        font_name: root.text_font_name
        halign: "center"
        valign: "middle"
        color: root.continent_colors[0] if root.cursor_position >= 0 else GRAY

    # Continent name 2
    Label:
        text: root.short_continents_list[1]
        font_size: root.font_ratio * root.font_size
        size_hint: (1/6, 0.8)
        pos_hint: {"x": 1/6, "center_y": 0.5}
        font_name: root.text_font_name
        halign: "center"
        valign: "middle"
        color: root.continent_colors[1] if root.cursor_position >= 1 else GRAY
    
    # Continent name 3
    Label:
        text: root.short_continents_list[2]
        font_size: root.font_ratio * root.font_size
        size_hint: (1/6, 0.8)
        pos_hint: {"x": 2/6, "center_y": 0.5}
        font_name: root.text_font_name
        halign: "center"
        valign: "middle"
        color: root.continent_colors[2] if root.cursor_position >= 2 else GRAY
    
    # Continent name 4
    Label:
        text: root.short_continents_list[3]
        font_size: root.font_ratio * root.font_size
        size_hint: (1/6, 0.8)
        pos_hint: {"x": 3/6, "center_y": 0.5}
        font_name: root.text_font_name
        halign: "center"
        valign: "middle"
        color: root.continent_colors[3] if root.cursor_position >= 3 else GRAY

    # Continent name 5
    Label:
        text: root.short_continents_list[4]
        font_size: root.font_ratio * root.font_size
        size_hint: (1/6, 0.8)
        pos_hint: {"x": 4/6, "center_y": 0.5}
        font_name: root.text_font_name
        halign: "center"
        valign: "middle"
        color: root.continent_colors[4] if root.cursor_position >= 4 else GRAY
    
    # Continent name 6
    Label:
        text: root.short_continents_list[5]
        font_size: root.font_ratio * root.font_size
        size_hint: (1/6, 0.8)
        pos_hint: {"x": 5/6, "center_y": 0.5}
        font_name: root.text_font_name
        halign: "center"
        valign: "middle"
        color: root.continent_colors[5] if root.cursor_position >= 5 else GRAY